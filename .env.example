# OpenAI importer
OPENAI_API_KEY=
OPENAI_IMPORT_MODEL=gpt-4.1

# Neon Postgres (required in Vercel production)
DATABASE_URL=

# Vercel Blob (required for photo upload + source snapshot storage in production)
BLOB_READ_WRITE_TOKEN=
